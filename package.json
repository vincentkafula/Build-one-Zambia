{
  "name": "boz-monorepo",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && node src/index.js",
    "build": "echo 'no build step'"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
