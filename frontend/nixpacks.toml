[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --production=false"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"

[variables]
NIXPACKS_CACHE_KEY = "redeploy-candidate-cards-fix-20260723"
