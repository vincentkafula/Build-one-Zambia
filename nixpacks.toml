[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "cd backend && npm install",
  "if [ \"$IS_BACKEND\" != \"true\" ]; then cd frontend && npm install && VITE_API_URL='' npm run build; fi"
]

[start]
cmd = "node server.js"
