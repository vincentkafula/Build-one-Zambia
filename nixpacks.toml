[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build && cp -r dist ../dist && cp server.js ../server.js"]

[start]
cmd = "node server.js"
